# Ephemeral — regenerated by build.sh / the Godot toolchain, never committed (committed-vs-ephemeral rule).
local-nuget/        # the local NuGet feed populated by build.sh (dotnet pack output)
.godot/             # Godot's import cache / generated project data
bin/
obj/
export/             # Godot export output
*.tmp
