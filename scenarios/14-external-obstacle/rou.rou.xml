<?xml version="1.0" encoding="UTF-8"?>
<routes xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://sumo.dlr.de/xsd/routes_file.xsd">
    <!-- B1 behavioral-test fixture: a single follower on a 500 m single lane. There is NO SUMO
         golden here (the obstacle is an external live input, not in any SUMO run). Tests inject an
         external obstacle via IEngine.AddObstacle and assert behavioral/property invariants
         (no overlap; holds at the Krauss steady gap = scenario 13's 242.499; resumes on removal). -->
    <vType id="car" vClass="passenger" sigma="0"/>
    <route id="r0" edges="e0"/>
    <vehicle id="follower" type="car" route="r0" depart="0" departPos="0" departSpeed="0" departLane="0"/>
</routes>
