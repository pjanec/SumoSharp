sumo_version=1.20.0
sumo_version_reported=Eclipse SUMO sumo Version 1.20.0
command=sumo -c config.sumocfg --fcd-output golden.fcd.xml --fcd-output.acceleration --precision 6 --save-state.times 1 --save-state.files golden.state.xml --no-step-log true
state_time=1
fcd_precision=6
# C11 anchor: IDM (Intelligent Driver Model) car-following. net = scenario 01's 1000m single lane.
#   Both vehicles carFollowModel="IDM"; leader maxSpeed=6 (slow), follower default desired (~13.9).
#   Follower departs behind (pos 0) the leader (pos 200), free-accelerates via IDM to ~13.7, then
#   brakes (IDM gap term) and settles following the slow leader at 6.0 m/s at the IDM equilibrium
#   gap. sigma=0, Euler, actionStepLength=1.
# IDM params (MSCFModel_IDM.cpp:38-44): delta=4, iterations=MAX2(1,int(TS/stepping(0.25)+0.5))=4,
#   twoSqrtAccelDecel=2*sqrt(accel*decel)=2*sqrt(2.6*4.5)=6.8410, tau(headway)=1, adaptationFactor=1.

# T2 (W3 snapshot cross-check) mid-run state:
state_mid_command=sumo -c config.sumocfg --save-state.times 30 --save-state.files golden.state.mid.xml --precision 6 --no-step-log true
state_mid_time=30 (compared against engine.Run(29).SaveSnapshot; SUMO saves the state ENTERING step 30)
