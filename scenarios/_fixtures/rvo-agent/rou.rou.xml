<?xml version="1.0" encoding="UTF-8"?>
<routes xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://sumo.dlr.de/xsd/routes_file.xsd">
    <!-- one centred vehicle on a 7.2 m lane; the test injects a finite-width external agent ahead
         and asserts the vehicle avoids it via the SAME RVO solve (Stage 3 unification). No golden. -->
    <vType id="car" vClass="passenger" sigma="0"/>
    <route id="r0" edges="e0"/>
    <vehicle id="v0" type="car" route="r0" depart="0" departPos="0" departSpeed="0"/>
</routes>
